# GitHub 配置完整指南

## 快速配置步骤

### 方法 1: 使用配置脚本（推荐）

运行配置脚本：
```bash
./setup-github.sh
```

脚本会自动引导你完成：
- Git 用户信息配置
- SSH 密钥生成
- GitHub SSH 连接测试

### 方法 2: 手动配置

#### 1. 配置 Git 用户信息

```bash
# 设置你的 GitHub 用户名
git config --global user.name "你的GitHub用户名"

# 设置你的 GitHub 邮箱
git config --global user.email "your-email@example.com"
```

#### 2. 生成 SSH 密钥

```bash
# 生成新的 SSH 密钥（推荐使用 ed25519）
ssh-keygen -t ed25519 -C "your-email@example.com" -f ~/.ssh/id_ed25519_github

# 或者使用 RSA（如果不支持 ed25519）
ssh-keygen -t rsa -b 4096 -C "your-email@example.com" -f ~/.ssh/id_rsa_github
```

#### 3. 将 SSH 密钥添加到 ssh-agent

```bash
# 启动 ssh-agent
eval "$(ssh-agent -s)"

# 添加密钥到 ssh-agent
ssh-add ~/.ssh/id_ed25519_github
# 或
ssh-add ~/.ssh/id_rsa_github
```

#### 4. 配置 SSH config（可选但推荐）

创建或编辑 `~/.ssh/config` 文件：

```bash
nano ~/.ssh/config
```

添加以下内容：

```
Host github.com
    HostName github.com
    User git
    IdentityFile ~/.ssh/id_ed25519_github
    IdentitiesOnly yes
```

#### 5. 将公钥添加到 GitHub

1. 复制公钥内容：
   ```bash
   cat ~/.ssh/id_ed25519_github.pub
   # 或
   cat ~/.ssh/id_rsa_github.pub
   ```

2. 打开 GitHub: https://github.com/settings/keys
3. 点击 "New SSH key"
4. 填写：
   - Title: EBYZ-你的电脑名称
   - Key: 粘贴刚才复制的公钥
5. 点击 "Add SSH key"

#### 6. 测试 SSH 连接

```bash
ssh -T git@github.com
```

如果成功，你会看到类似以下消息：
```
Hi YOUR_USERNAME! You've successfully authenticated...
```

## 连接 GitHub 仓库

配置完成后，将本地仓库连接到 GitHub：

```bash
# 添加远程仓库（使用 SSH）
git remote add origin git@github.com:YOUR_USERNAME/EBYZ.git

# 或者使用 HTTPS（如果未配置 SSH）
git remote add origin https://github.com/YOUR_USERNAME/EBYZ.git

# 检查远程仓库
git remote -v

# 推送代码
git push -u origin main
```

## 常用 Git 命令

```bash
# 查看状态
git status

# 添加更改
git add .

# 提交更改
git commit -m "你的提交信息"

# 推送到 GitHub
git push

# 拉取最新更改
git pull

# 查看提交历史
git log --oneline
```

## 故障排除

### 如果 SSH 连接失败

1. 检查 SSH 密钥是否已添加到 GitHub
2. 测试连接：`ssh -T git@github.com`
3. 检查 `~/.ssh/config` 配置是否正确
4. 确认使用的是正确的密钥文件

### 如果推送需要密码

- 确保使用了 SSH URL (`git@github.com:...`) 而不是 HTTPS
- 检查 SSH 密钥是否正确配置
- 或者使用 Personal Access Token 代替密码（HTTPS 方式）

## 使用 Personal Access Token（HTTPS 方式）

如果不想使用 SSH，可以使用 Personal Access Token：

1. 创建 Token: https://github.com/settings/tokens
2. 生成新 token，选择 `repo` 权限
3. 使用 HTTPS URL 时，用户名使用 GitHub 用户名，密码使用 Token

